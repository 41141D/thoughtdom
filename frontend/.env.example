# Existing backend API base (unchanged by the Supabase integration).
NEXT_PUBLIC_API_URL=http://localhost:8000

# Documented per project requirements. Not currently read by any frontend
# code: image URLs already arrive as full Supabase Storage public URLs in
# API responses, and lib/api.ts's mediaUrl() already passes absolute URLs
# through unchanged. Reserved for future direct-Supabase frontend use.
# Safe to expose (anon-scoped) -- never put the service_role key here.
NEXT_PUBLIC_SUPABASE_URL=https://ulocbrcwromgbsnaxsbw.supabase.co
